version: 0.2

phases:
  build:
    commands:
    - chmod a+x ./gradlew
    - ./gradlew bootJar
  post_build:
    commands:
    - echo Build completed on `date`
    - aws s3 cp build/libs/simpleservice*.jar s3://codepipeline-eu-central-1-686850500167/application.jar